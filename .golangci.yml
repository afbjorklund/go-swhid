---
version: "2"
linters:
  enable:
    - govet
    - gocyclo
  exclusions:
    rules:
      - source: "^\\s*defer\\s+"
        linters: ["errcheck"]
      - source: "WriteString\\(fmt.Sprintf\\(.*\\)\\)"
        linters: ["staticcheck"]
formatters:
  enable:
    - gofmt
    - goimports
